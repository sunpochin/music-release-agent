# music-release-agent 开发指南

已獨立的音樂釋出掃描與分析代理程式，專注做三件事：

- Spotify OAuth 與關注藝人新發行掃描
- AI 生成繁體中文樂評
- 將樂評寫入本地 `gitbook/` 並透過 Git 推送

## 結構

- `server.js`: Spotify OAuth callback server
- `scan-releases.js`: 一鍵執行掃描、評論與發布流程
- `src/spotify-auth.js`: Spotify token 管理
- `src/spotify-client.js`: Spotify / MusicBrainz 掃描與降級邏輯
- `src/album-reviewer.js`: Gemini / Ollama 樂評生成
- `src/gitbook-publisher.js`: GitBook 檔案與 GitOps 發布

## 快速開始

```bash
npm install
cp .env.example .env
npm run dev
```

打開：

- `http://localhost:3011/login/spotify`

授權成功後執行：

```bash
npm run scan
```

## 注意

- `spotify_tokens.json`、`data/scanner-state.json`、`data/system-state.json` 都是本地狀態，不會提交。
- `gitbook/` 是輸出內容，會被 `src/gitbook-publisher.js` 維護與提交。
- `scan-releases.js` 依賴目前 repo 是 Git 工作樹，且已設定好遠端。

## Spotify 禮貌掃描與極限分析

本專案實施了最嚴格的 Spotify API 防禦性禮貌掃描機制，旨在於 24 小時內完成一次完整掃描，同時保證絕不因高頻率請求而觸發 Spotify 封鎖。

### 1. 核心禮貌機制設計
* **動態分批尺寸 (Dynamic Batch Sizing)**：
  每次掃描的批次大小公式為 `Math.max(15, Math.ceil(total_artists / 8))`。
  配合 PM2 設定每 3 小時執行一次（24 小時共執行 8 次），此公式可確保不論關注藝人數量多少，**必定能在 24 小時內將所有藝人輪流掃描一遍**。
* **隨機防禦性延遲 (Politeness Delay)**：
  在對每個藝人發送請求前，隨機等待 `1000ms ~ 2000ms`（平均 `1500ms`）。
* **藝人掃描後冷卻 (Post-Artist Cooldown)**：
  每掃完一位藝人，強制休眠 `1000ms`。
* **全域限速閥門 (Global Bottleneck Throttle)**：
  所有對 Spotify API 的請求之間均強制間隔至少 `1000ms`（由全域排隊 Mutex 鎖 `spotifyLock` 控制）。
* **雙源自動降級**：
  若不幸遭遇 429 Rate Limit，系統會自動在該批次中降級改為查詢 `MusicBrainz` API，並在 24 小時內累計 429 達到 2 次時，啟動 24 小時強制冷卻保護。

---

### 2. 藝人規模與掃描時間極限分析

依據上述「禮貌機制」，平均掃描一位藝人（含隨機等待與冷卻）約需花費 **`2.8 秒`**。

#### A. 109 位藝人（目前規模）
* **每次執行數量**：`Math.max(15, Math.ceil(109 / 8)) = 15` 位藝人。
* **每次執行時間**：`15 * 2.8 秒 = 42 秒`。
* **結論**：極度輕鬆安全，每天總運行時間約 5.6 分鐘。

#### B. 2,000 位藝人（中大型關注量）
* **每次執行數量**：`Math.ceil(2000 / 8) = 250` 位藝人。
* **每次執行時間**：`250 * 2.8 秒 = 700 秒`（約 **`11.6 分鐘`**）。
* **結論**：完全可行！每次執行僅需 11.6 分鐘，遠低於 PM2 的 3 小時（180 分鐘）排程間隔。24 小時內可在極佳的 API 禮貌狀態下完成 2,000 位藝人的完整掃描，且不會造成任務堆積。

#### C. 5,000 位藝人（Spotify 官方硬性上限）
* **每次執行數量**：`Math.ceil(5000 / 8) = 625` 位藝人。
* **每次執行時間**：`625 * 2.8 秒 = 1750 秒`（約 **`29 分鐘`**）。
* **結論**：可行。這已達 Spotify 個人帳號關注藝人的官方硬性限制（Spotify 限制單一帳號關注上限為 5,000 人）。在此極限下，每次執行耗時約 29 分鐘，依然在 3 小時排程間隔安全範圍內。

#### D. 65,535 位藝人（極端想像情況）
如果我們打破 Spotify 限制（改用外部藝人清單而非 API 關注清單）掃描 65,535 位藝人：
* **每次執行數量**：`Math.ceil(65535 / 8) = 8192` 位藝人。
* **在最禮貌延遲下的每次執行時間**：`8192 * 2.8 秒 = 22937.6 秒`（約 **`6.37 小時`**）。
* **瓶頸與衝突**：
  1. **執行重疊 (Overlap)**：每次執行需要 6.37 小時，而我們的 PM2 排程是每 3 小時執行一次，這會造成前一次掃描尚未結束，下一次掃描就被重啟觸發，導致任務互相干涉與系統阻塞。
  2. **時間赤字 (Time Deficit)**：24 小時內以該禮貌延遲掃完 65535 位藝人，總共需要 `8 * 6.37 = 51 小時` 的運作時間。由於物理上一天只有 24 小時，**在 24 小時內不可能以如此禮貌的延遲掃完 65,535 人**。
  3. **429 與封鎖風險**：若要強行在 24 小時內掃完，必須將每位藝人的掃描延遲降到 `0.3 秒` 以下。如此高的併發與請求頻率，將在幾分鐘內觸發 Spotify 的 API 全域 Rate Limit，進而導致帳號/IP 被封鎖。
* **建議方案**：若未來有如此龐大的藝人資料庫需求，應考慮以下架構調整：
  * 放寬掃描週期（例如改為 7 天完整掃描一次，而非 24 小時）。
  * 導入多帳號 API 憑證輪替 (Credential Rotation) 與代理伺服器 (Proxy Pool) 分散請求。
  * 依據藝人活躍度（例如半年內是否有發新歌）實施動態過濾，排除長期未發行新作品的藝人，降低掃描總量。

